export interface IDecodedToken {
    username: string,
    user_id: string
}

export interface ILoginInfo {
    username: string,
    password: string
}

export interface IIngredient {
    id: string,
    amount: Number
}